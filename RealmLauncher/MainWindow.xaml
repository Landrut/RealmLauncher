<Window x:Class="RealmLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:RealmLauncher.Views"
        Title="REALM RolePlay Launcher"
        Width="900"
        Height="600"
        MinWidth="900"
        MinHeight="600"
        MaxWidth="900"
        MaxHeight="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        FontFamily="{DynamicResource AppFont}"
        Background="Transparent">
    <Window.Resources>
        <FontFamily x:Key="AppFont">pack://application:,,,/Assets/Fonts/#TT Norms Pro</FontFamily>
        <SolidColorBrush x:Key="TextMain" Color="#DBE5F5"/>
        <SolidColorBrush x:Key="TextMuted" Color="#9FB4D9"/>
        <SolidColorBrush x:Key="PanelFill" Color="#2F4D8D" Opacity="0.55"/>
        <SolidColorBrush x:Key="InputBg" Color="#0D1A34"/>
        <SolidColorBrush x:Key="InputStroke" Color="#2B426D"/>

        <Style x:Key="PillButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#4A87EA"/>
            <Setter Property="BorderBrush" Value="#031531"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                CornerRadius="15"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#5A95F8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TextInputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputStroke}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PasswordInputStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource InputStroke}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border CornerRadius="16"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RealmCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Mark" Width="18" Height="18" CornerRadius="6" Background="#D9D9D9" BorderBrush="#020617" BorderThickness="1" Margin="0,0,8,0">
                                <Path x:Name="Tick" Data="M3,9 L7,13 L15,5" Stroke="#DBE5F5" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Mark" Property="Background" Value="#4A87EA"/>
                                <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="17" Background="#132D5F" SnapsToDevicePixels="True">
    <Grid Background="#132D5F" ClipToBounds="True">
        <Path Data="M891.429 0H8.57143C3.83756 0 0 8.9543 0 20V580C0 591.046 3.83756 600 8.57143 600H891.429C896.162 600 900 591.046 900 580V20C900 8.9543 896.162 0 891.429 0Z"
              Stretch="Fill"
              Opacity="0.35">
            <Path.Fill>
                <LinearGradientBrush MappingMode="Absolute" StartPoint="80,-44" EndPoint="900,600">
                    <GradientStop Color="#EA020617" Offset="0"/>
                    <GradientStop Color="#B81D75FA" Offset="0.47"/>
                    <GradientStop Color="#B41A6BE4" Offset="0.53"/>
                    <GradientStop Color="#EA020617" Offset="1"/>
                </LinearGradientBrush>
            </Path.Fill>
        </Path>

        <Border BorderBrush="#1E355F" BorderThickness="1" CornerRadius="17"/>
        <Rectangle Height="2" Fill="#031531" VerticalAlignment="Top" Margin="0,76,0,0"/>
        <Rectangle Height="54" Fill="#010C2A" VerticalAlignment="Bottom"/>

        <Grid Height="78" VerticalAlignment="Top" MouseLeftButtonDown="HeaderBar_OnMouseLeftButtonDown">
            <Image x:Name="imgLogo" Width="146" Height="50" Margin="20,15,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"/>

            <Button x:Name="btnOpenSettings"
                    Width="180"
                    Margin="0,16,58,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Content="Настройки"
                    Style="{StaticResource PillButtonStyle}"
                    Click="BtnOpenSettings_OnClick"/>

            <Button x:Name="btnBackToMain"
                    Width="180"
                    Margin="0,16,58,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Content="Главное меню"
                    Style="{StaticResource PillButtonStyle}"
                    Visibility="Collapsed"
                    Click="BtnBackToMain_OnClick"/>

            <Button x:Name="btnCloseApp"
                    Width="34"
                    Height="34"
                    Margin="0,14,12,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Content="✕"
                    ToolTip="Закрыть"
                    Style="{StaticResource PillButtonStyle}"
                    FontSize="16"
                    Click="BtnCloseApp_OnClick"/>
        </Grid>

        <Grid Margin="0,78,0,54">
            <views:MainPageView x:Name="MainPage"/>
            <views:SettingsPageView x:Name="SettingsPage" Visibility="Collapsed"/>
        </Grid>
    </Grid>
    </Border>
</Window>
